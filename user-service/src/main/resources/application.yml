feign:
  client:
    config:
      document:
        url: ${document.url}
        loggerLevel: basic
        connectTimeout: 5000
        readTimeout: 2000
        errorDecoder: com.gabrielfeitosa.user.decoder.DocumentErrorDecode